# Normalise line endings to LF in the repo and on checkout.
#
# Without this, Windows checks files out as CRLF while the generator scripts in
# tools/ write LF, so a no-op run of tools/quizshuffle.js leaves the quiz banks
# showing as modified in `git status` with no actual content change.
* text=auto eol=lf
