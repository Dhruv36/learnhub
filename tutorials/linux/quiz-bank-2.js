// linux quiz bank — sets 6–10 (20 questions each, with explanations)
window.QUIZ_SETS = window.QUIZ_SETS || [];
window.QUIZ_SETS.push(
{ title: "Quiz 6 · Storage, Filesystems & Mounts",
  desc: "The block-device stack, mounting, full disks, durability and I/O performance.",
  questions: [
  { q: "What does mounting a filesystem do to the directory it is mounted on?", options: ["Hides them — they reappear when unmounted", "Merges the two sets of files", "Deletes the directory's existing contents"], answer: 0,
    explain: "This causes a real production bug: a service writes before its disk mounts, the mount happens, and the data vanishes from view while still consuming space on the parent filesystem." },
  { q: "Why should /etc/fstab identify devices by UUID rather than /dev/sdb?", options: ["/dev names do not work with LVM", "UUIDs are shorter", "Kernel device names are assigned in probe order and can change between boots, silently mounting the wrong disk"], answer: 2,
    explain: "LABEL= works too. And always test with `mount -a` before rebooting — a bad fstab entry drops a cloud VM into emergency mode with no console." },
  { q: "`df -h` shows 30 GB free but writes fail with 'No space left on device'. Why?", options: ["Inode exhaustion — check df -i", "The disk is mounted read-only", "The filesystem is corrupt"], answer: 0,
    explain: "A filesystem has two finite resources. ext4 allocates inodes at format time, and millions of small files exhaust them while bytes remain free." },
  { q: "`df` says the disk is full but `du` only accounts for 2 GB. What is happening?", options: ["A deleted file is still held open by a process — the blocks are not freed until the fd closes", "The filesystem needs fsck", "du is inaccurate on large filesystems"], answer: 0,
    explain: "`lsof +L1` finds it (NLINK 0). Truncate through /proc/PID/fd/N or use `: > file` next time instead of rm on a live log." },
  { q: "What does a successful write() actually guarantee?", options: ["Only that the data is in the kernel's page cache, marked dirty", "That the file's metadata is updated", "The data is on the physical device"], answer: 0,
    explain: "Writeback happens within ~30 seconds or under pressure. fsync() is what makes data durable, and it costs 0.1–10 ms depending on the device." },
  { q: "What is the correct atomic-file-update sequence?", options: ["Write directly over the target, then fsync", "Lock the file, write, unlock", "Write temp file → fsync temp → rename over target → fsync the DIRECTORY"], answer: 2,
    explain: "The final directory fsync is the step almost everyone omits — without it the rename itself can be lost in a crash even though the data was durable." },
  { q: "What does ext4 journaling protect?", options: ["Against disk hardware failure", "Filesystem metadata consistency, so the filesystem mounts cleanly — recent data can still be missing", "Your file contents after a crash"], answer: 1,
    explain: "This is why a journaled filesystem plus no fsync still loses the last few seconds of writes. Journaling is about structural integrity, not durability of your data." },
  { q: "Why is %util misleading on an NVMe device?", options: ["It reports time with at least one request in flight, but NVMe serves many requests in parallel — 100% does not mean saturated", "It is measured incorrectly", "It only counts write operations"], answer: 0,
    explain: "Use await (latency) and queue depth instead. A device at 100% util with 0.2 ms latency is doing exactly what it was bought for." },
  { q: "How do you distinguish a slow device from a device overwhelmed by the workload?", options: ["Measure latency at queue depth 1 — if it is low, the earlier high latency was queueing delay", "Compare throughput to the rated speed", "Check %util over a longer window"], answer: 0,
    explain: "`fio --iodepth=1 --direct=1` isolates intrinsic device latency from queueing, and that distinction decides whether you buy hardware or fix code." },
  { q: "Why can the same NVMe device deliver 3 GB/s and also 40 MB/s?", options: ["Different filesystems", "Thermal throttling", "Sequential versus 4 KB random access — a 25x+ difference on identical hardware"], answer: 2,
    explain: "A query plan that switched from a sequential scan to random index lookups looks exactly like a failing disk in the metrics." },
  { q: "What does `noatime` as a mount option do?", options: ["Speeds up file creation", "Stops updating the access time on every read, turning reads into pure reads instead of read+write", "Disables timestamps entirely"], answer: 1,
    explain: "relatime (the default) limits updates to once a day. noatime is a nearly free performance win on read-heavy filesystems." },
  { q: "Which mount options are appropriate for /tmp?", options: ["sync,dirsync", "nosuid,nodev,noexec", "ro,noatime"], answer: 1,
    explain: "A world-writable directory should not honour setuid bits, device files, or execution — three attack vectors closed with one line." },
  { q: "Why does ext4 reserve 5% of blocks for root by default?", options: ["For journal storage", "So root and system daemons can still write when the filesystem appears full to ordinary users", "To reduce fragmentation"], answer: 1,
    explain: "On a 4 TB data volume that reserve is 200 GB wasted — `tune2fs -m 1` is the standard fix for non-root filesystems." },
  { q: "Which operation is NOT supported on XFS?", options: ["Repair with xfs_repair", "Shrinking the filesystem", "Online growth"], answer: 1,
    explain: "XFS can never shrink, which makes size decisions one-way. ext4 can shrink but only offline, with the filesystem unmounted." },
  { q: "What is the correct order to grow an ext4 filesystem on LVM?", options: ["resize2fs, then lvextend", "lvextend to grow the logical volume, then resize2fs to grow the filesystem", "Only lvextend is needed"], answer: 1,
    explain: "The volume must be bigger before the filesystem can expand into it. For XFS the second step is xfs_growfs, called with the mountpoint rather than the device." },
  { q: "Why must fsck never run on a mounted filesystem?", options: ["It requires exclusive network access", "It would be too slow", "The tool and the kernel would modify the same structures concurrently, corrupting it"], answer: 2,
    explain: "Unmount first, or boot to rescue media. And fsck repairs bookkeeping, never failing hardware — SMART tells you about that." },
  { q: "What is an LVM snapshot useful for?", options: ["Permanent backups", "Reducing disk usage", "A consistent point-in-time view for backing up a live database without a long lock"], answer: 2,
    explain: "It has a fixed size for changed blocks; if writes during the backup window exceed it, the snapshot is dropped and the backup fails." },
  { q: "What does `dd if=/dev/zero of=file bs=1M count=1000` actually measure without conv=fsync?", options: ["Page-cache (memory) write speed — often 10x the real device throughput", "The disk's write speed", "Filesystem metadata overhead"], answer: 0,
    explain: "conv=fsync forces the data to the device before dd exits, giving an honest number. Without it you are benchmarking memcpy." },
  { q: "What is the fastest way to identify which directory has exhausted a filesystem's inodes?", options: ["find /path -xdev -type f -printf '%h\\n' | sort | uniq -c | sort -rn", "du -sh on every directory", "ls -R | wc -l"], answer: 0,
    explain: "Counting files per parent directory finds the offender immediately. -xdev keeps it on one filesystem, which matters when /var is separate." },
  { q: "Why is `rm *` unsuitable for a directory with two million files?", options: ["The shell expands the glob into a single argument list that exceeds the kernel's limit (E2BIG)", "rm requires confirmation for each file", "rm is too slow"], answer: 0,
    explain: "`find . -type f -delete` or `find -print0 | xargs -0 rm` avoids building one enormous argument list." },
]},
{ title: "Quiz 7 · Networking From the Socket Up",
  desc: "Sockets, ss, connection state, backlog queues, TIME_WAIT and DNS resolution.",
  questions: [
  { q: "What is a socket, from the process's point of view?", options: ["A special file on disk", "A kernel thread", "A file descriptor — read/write/close work on it just like a file"], answer: 2,
    explain: "That uniformity is why shell redirection composes with network endpoints, e.g. `exec 3<>/dev/tcp/host/port` in bash." },
  { q: "Why prefer a Unix domain socket over 127.0.0.1 for local IPC?", options: ["It skips the entire TCP/IP stack and is protected by filesystem permissions rather than by hoping nothing else connects", "It supports more connections", "It is the only option for databases"], answer: 0,
    explain: "No checksums, no loopback routing, no ephemeral port consumption — measurably faster and more secure for same-host communication." },
  { q: "Why did ss replace netstat?", options: ["netstat was removed from the kernel", "ss reads socket state over netlink instead of parsing /proc/net/tcp line by line, which matters enormously at scale", "ss supports IPv6 and netstat does not"], answer: 1,
    explain: "On a host with 100,000 connections netstat can take minutes where ss takes milliseconds." },
  { q: "What is the security significance of a service bound to 0.0.0.0 versus 127.0.0.1?", options: ["No difference if a firewall is present", "0.0.0.0 is reachable from the network; 127.0.0.1 is unreachable from other hosts regardless of firewall configuration", "127.0.0.1 is slower"], answer: 1,
    explain: "Auditing bind addresses with one ss command finds a whole class of serious exposure — unauthenticated databases on the public interface." },
  { q: "What two queues does listen() create?", options: ["Inbound and outbound queues", "Read and write queues", "The SYN queue (half-open connections) and the accept queue (completed, awaiting accept())"], answer: 2,
    explain: "Accept-queue overflow is the nastier failure: the handshake completed so the client believes it is connected, then the request hangs — and the application logs nothing." },
  { q: "On a LISTEN socket, what do Recv-Q and Send-Q in `ss -tln` show?", options: ["Bytes buffered for reading and writing", "Retransmission counters", "The current accept-queue depth and its maximum"], answer: 2,
    explain: "Recv-Q sitting at Send-Q means it is overflowing right now, and `nstat ListenOverflows` counts the connections being dropped." },
  { q: "Why does raising net.core.somaxconn alone often fail to fix backlog overflow?", options: ["The application's own listen(fd, backlog) argument still caps the queue — the effective size is the minimum of the two", "somaxconn only affects UDP", "It requires a reboot"], answer: 0,
    explain: "Both must be raised. And sustained overflow is a capacity signal, not a tuning problem — a deeper queue converts fast failures into slow ones." },
  { q: "Which side of a TCP connection enters TIME_WAIT?", options: ["The server, always", "Whichever side closes first", "Both sides"], answer: 1,
    explain: "This is why letting HTTP clients close first moves the TIME_WAIT burden to many clients rather than concentrating it on one server." },
  { q: "When is a large number of TIME_WAIT sockets genuinely a problem?", options: ["On the client side, where each one holds an ephemeral port and can exhaust the outbound port range to a single destination", "On the server side, because the listening port is blocked", "Whenever it exceeds 10,000"], answer: 0,
    explain: "Server-side TIME_WAIT costs only memory. About 28,000 ports over a 60-second window caps outbound connections near 470/s to one destination." },
  { q: "What is the correct fix for ephemeral port exhaustion?", options: ["Widening ip_local_port_range", "tcp_tw_recycle=1", "Connection pooling / HTTP keep-alive — reusing connections instead of creating them"], answer: 2,
    explain: "tcp_tw_reuse is a safe mitigation and widening the range buys ~2x, but only reuse addresses the cause. tcp_tw_recycle broke NAT and was removed in kernel 4.12." },
  { q: "Why should you use `getent hosts` rather than `dig` to reproduce an application's DNS resolution?", options: ["dig queries DNS directly, bypassing /etc/hosts, nsswitch ordering, systemd-resolved and search domains", "getent is faster", "dig is deprecated"], answer: 0,
    explain: "'dig works but the app can't resolve it' is almost always one of those four, and dig can never reproduce it." },
  { q: "What does `options ndots:5` in resolv.conf cause?", options: ["Any name with fewer than five dots is tried against every search domain first, generating several failed lookups", "A five-second timeout", "Five retries per query"], answer: 0,
    explain: "The Kubernetes default. A trailing dot (api.example.com.) marks the name absolute and skips the search list entirely." },
  { q: "What does 'Connection refused' prove?", options: ["Packets reached the host and it replied with RST — so DNS, routing and firewall are all fine, and the service is the problem", "The host is down", "A firewall is blocking the port"], answer: 0,
    explain: "The counterpart: a timeout means nothing came back, which is a silent drop — firewall, security group, or wrong route." },
  { q: "You see repeated SYNs with no reply in tcpdump. What does that indicate?", options: ["The server's accept queue is full", "The service crashed", "Packets are being dropped silently — a DROP firewall rule, security group, or routing problem"], answer: 2,
    explain: "A stopped service produces an immediate RST. Silence is the signature of a drop, which is exactly what firewalls do by design." },
  { q: "What does `ss -tlnp` show that `ss -tln` does not?", options: ["Only listening sockets", "Numeric addresses", "The owning process and PID (requires root for other users' sockets)"], answer: 2,
    explain: "-t TCP, -l listening, -n numeric (skips DNS lookups and is much faster), -p process." },
  { q: "Why can `ping` failing be misleading?", options: ["ping uses UDP", "ICMP is frequently blocked while TCP works fine — test the actual port instead", "ping only works on the local subnet"], answer: 1,
    explain: "`nc -zv host port` or `timeout 2 bash -c '</dev/tcp/host/port'` tests what you actually care about." },
  { q: "What does SO_REUSEADDR solve?", options: ["Letting a restarted server rebind a port that still has connections in TIME_WAIT — the classic 'Address already in use' on redeploy", "Port exhaustion", "Sharing a port between processes"], answer: 0,
    explain: "It addresses the restart case specifically; it does not help with client-side ephemeral port exhaustion." },
  { q: "What is the significance of `[NOTFOUND=return]` in nsswitch.conf's hosts line?", options: ["It caches negative results", "If the preceding source returns NOTFOUND, resolution stops and later sources (like dns) are never consulted", "It logs failed lookups"], answer: 1,
    explain: "A real cause of intermittent resolution failures that dig can never reproduce, because dig does not consult nsswitch at all." },
  { q: "What does a rising `nstat TcpExtListenOverflows` counter mean?", options: ["Retransmissions are occurring", "Fully-established connections were dropped because the application was too slow to accept() them", "SYN flood attack"], answer: 1,
    explain: "The application never sees these connections, so nothing appears in its logs — which is what makes the failure so hard to diagnose." },
  { q: "What is the first step when debugging 'the service can't connect'?", options: ["Check the application logs", "Restart the service", "Walk the layers in order — name resolution, route, reachability, port, listener, bind address, firewall — and stop at the first failure"], answer: 2,
    explain: "Each layer has one command and one unambiguous answer, which converts guessing into a search that terminates." },
]},
{ title: "Quiz 8 · systemd, Logging & Time",
  desc: "Units, dependencies, the journal, log rotation, clocks and NTP.",
  questions: [
  { q: "What are PID 1's two irreplaceable responsibilities?", options: ["Reaping orphaned children and starting/supervising services", "Handling interrupts and managing the page cache", "Scheduling and memory management"], answer: 0,
    explain: "A container whose PID 1 does neither accumulates zombies and ignores SIGTERM — which is exactly what `docker run --init` fixes." },
  { q: "Why must you run `systemctl daemon-reload` after editing a unit file?", options: ["To validate the syntax", "To restart the service", "systemd caches unit definitions; without a reload it keeps running the old version and your edit appears to do nothing"], answer: 2,
    explain: "The most common systemd frustration, and the reason `systemctl edit` runs daemon-reload for you." },
  { q: "Where should you put a customisation of a packaged unit?", options: ["Edit /lib/systemd/system/name.service directly", "A drop-in via `systemctl edit`, which lands in /etc/systemd/system/name.service.d/", "Copy it to ~/.config/systemd/"], answer: 1,
    explain: "Editing the packaged file means losing the change at the next package update. `systemctl cat` shows the base unit plus every drop-in in order." },
  { q: "What is the difference between After= and Requires=?", options: ["Requires= implies After=", "After= controls ordering only; Requires= controls whether the dependency is started and whether failure propagates", "They are synonyms"], answer: 1,
    explain: "Requires= without After= is a race that usually passes on a warm restart and fails at boot — the worst possible failure signature." },
  { q: "Why is Type=simple risky for a service with slow startup?", options: ["It cannot restart automatically", "It does not capture stdout", "systemd marks it started the instant it forks — before the port is bound — so dependents race it"], answer: 2,
    explain: "Type=notify with sd_notify(READY=1) reports genuine readiness. Type=exec at least confirms execve succeeded." },
  { q: "What does network-online.target mean that network.target does not?", options: ["Nothing, they are aliases", "That an address is actually configured — network.target only means the stack is being set up", "That DNS is working"], answer: 1,
    explain: "A service that binds a specific IP needs Wants= plus After= on network-online.target, and a wait-online service must be enabled for the target to mean anything." },
  { q: "What happens when a service hits StartLimitBurst within StartLimitIntervalSec?", options: ["It escalates to a reboot", "systemd gives up and leaves the unit in a failed state until `systemctl reset-failed`", "It restarts with a longer delay"], answer: 1,
    explain: "This is a feature: a service that failed five times in five minutes will not fix itself on the sixth, and a failed unit is a clear alert rather than noise." },
  { q: "Why must TimeoutStopSec exceed your longest in-flight request?", options: ["To allow the journal to flush", "systemd sends SIGTERM then SIGKILL after the timeout — killing mid-request drops in-flight work", "To prevent a restart loop"], answer: 1,
    explain: "For anything transactional this is a correctness issue, not a tidiness one." },
  { q: "What does `systemd-analyze security myapp.service` do?", options: ["Scans the binary for vulnerabilities", "Grades the unit's sandboxing exposure out of 10 and names the directives that would improve it", "Checks file permissions"], answer: 1,
    explain: "The fastest hardening feedback loop available — going from 9.6 UNSAFE to under 2.0 typically takes fifteen minutes of drop-in directives." },
  { q: "If /var/log/journal/ does not exist, where does journald store logs?", options: ["/run/log/journal — tmpfs, so everything is lost at reboot", "/var/log/syslog", "Nowhere; logging is disabled"], answer: 0,
    explain: "The default in several distributions and most container images, invariably discovered right after the crash you needed to investigate." },
  { q: "Which journalctl invocation is most useful after an unexplained reboot?", options: ["journalctl -b -1", "journalctl --since today", "journalctl -f"], answer: 0,
    explain: "The evidence is in the boot that ended, not the current one. `--list-boots` shows what is available." },
  { q: "What does `journalctl -p err` return?", options: ["Entries of priority err and more severe (crit, alert, emerg)", "Entries containing the word 'error'", "Only entries of priority exactly err"], answer: 0,
    explain: "Syslog priorities run 0 (emerg) to 7 (debug), and priority filters are always 'this level and worse'." },
  { q: "A daemon's log file has been empty since midnight while the service runs fine. Why?", options: ["logrotate renamed the file and the daemon still holds the old inode open — it is writing to app.log.1", "Log level was changed", "The disk is full"], answer: 0,
    explain: "`lsof` shows the process holding the renamed file. The fix is a postrotate reload; the permanent fix is logging to stdout." },
  { q: "What is the downside of logrotate's copytruncate?", options: ["It is slower", "It requires root", "Any line written between the copy and the truncate is lost silently"], answer: 2,
    explain: "Prefer postrotate + reload so the daemon reopens by name. copytruncate is a last resort for daemons that genuinely cannot reopen." },
  { q: "Why is logging to stdout the recommended approach for a service?", options: ["The supervisor captures it with unit/PID/boot metadata, and rotation, permissions and reopen signals stop being your problem", "It uses less disk space", "It is faster than file I/O"], answer: 0,
    explain: "The twelve-factor rule exists because it removes an entire category of bugs rather than mitigating them." },
  { q: "Which clock should a timeout use, and why?", options: ["Either — they behave identically", "CLOCK_REALTIME, because it reflects actual time", "CLOCK_MONOTONIC, because wall-clock time can jump forwards or backwards"], answer: 2,
    explain: "A backwards NTP step makes a wall-clock timeout hang for hours; a forwards step makes it fire instantly. Monotonic time only moves forward." },
  { q: "Why does NTP slew rather than step the clock during normal operation?", options: ["Slewing is more accurate", "A backwards jump breaks code holding wall-clock deadlines, corrupts log ordering, and can violate database assumptions", "Stepping is slower"], answer: 1,
    explain: "chrony's `makestep 1.0 3` encodes the policy: step only during the first few updates at boot, slew forever after." },
  { q: "Should a server's hardware RTC store UTC or local time?", options: ["Local time, to match the displayed clock", "It does not matter with NTP enabled", "UTC — 'RTC in local TZ' produces a wrong clock at every DST transition"], answer: 2,
    explain: "The local-RTC convention comes from Windows and causes dual-boot machines to shift by an hour twice a year." },
  { q: "Two hosts' logs show a response logged before its request. What should you check first?", options: ["Clock offset on both hosts — a few seconds of skew reverses cause and effect", "Whether the log files were rotated", "The application's logging library"], answer: 0,
    explain: "`chronyc tracking` per host. Beyond that, the durable fix is recording causality explicitly with correlation IDs and parent spans rather than inferring it from timestamps." },
  { q: "What does 'Suppressed 4821 messages' in the journal mean?", options: ["Duplicate messages were deduplicated", "Messages were forwarded to syslog", "journald's rate limiter dropped entries — the logs you are looking for may not be there"], answer: 2,
    explain: "Raise LogRateLimitIntervalSec/Burst for that specific unit rather than globally, so one noisy service cannot drown out everything else." },
]},
{ title: "Quiz 9 · Scripting, Performance & Containers",
  desc: "Robust shell scripts, the USE method, PSI, namespaces and cgroups.",
  questions: [
  { q: "Where does `set -e` NOT cause the script to exit on failure?", options: ["When the failing command is a builtin", "Inside functions", "In conditions, in && / || chains, in `result=$(cmd)` assignments, and in `local x=$(cmd)`"], answer: 2,
    explain: "These four are exactly where production scripts fail silently, which is why set -e is a floor rather than a guarantee." },
  { q: "Why does `local x=$(failing_cmd)` mask the failure even with set -e?", options: ["`local` itself returns 0, so its status replaces the command substitution's", "Command substitutions never fail", "local runs in a subshell"], answer: 0,
    explain: "Declare and assign separately: `local x; x=$(cmd) || return 1`. shellcheck flags this pattern." },
  { q: "What does `set -o pipefail` change?", options: ["It stops pipelines on the first failure", "A pipeline returns the first non-zero status instead of only the last command's", "It disables buffering in pipes"], answer: 1,
    explain: "Without it `false | true` reports success, so a failing first stage in a data pipeline is completely invisible." },
  { q: "Which trap signal reliably runs cleanup on every normal exit path?", options: ["ERR", "EXIT", "TERM"], answer: 1,
    explain: "EXIT fires on normal completion, explicit exit, and set -e failure. Capture $? as the handler's first line, or the trap's own commands overwrite it." },
  { q: "Why can't a trap protect against `kill -9`?", options: ["The trap runs but too slowly", "SIGKILL cannot be caught, blocked or ignored — nothing runs", "Traps only work in interactive shells"], answer: 1,
    explain: "Anything that must not leak needs a second mechanism: flock (kernel-released), systemd's RuntimeDirectory, or a boot-time sweep." },
  { q: "What makes flock the correct locking mechanism for a script?", options: ["It is the fastest", "The lock lives on a file descriptor and the kernel releases it when the process exits — including under SIGKILL", "It works across machines"], answer: 1,
    explain: "`[[ -f lockfile ]] && exit` is broken because checking and creating are not atomic, and a PID file needs careful stale-entry handling." },
  { q: "What does the USE method check for each resource?", options: ["Uptime, Speed, Errors", "Usage, Scaling, Efficiency", "Utilisation, Saturation, Errors"], answer: 2,
    explain: "Saturation matters most — queueing is what users feel — but errors are worth checking first because they are cheap and occasionally decisive." },
  { q: "Why is Linux's load average often misinterpreted?", options: ["It is per-core rather than total", "It counts uninterruptible (D-state) I/O-blocked tasks as well as runnable ones", "It is averaged over too long a window"], answer: 1,
    explain: "A load of 40 could be 40 threads wanting CPU or 2 running and 38 blocked on a hung NFS mount. `vmstat`'s r and b columns split it." },
  { q: "What does %iowait actually measure?", options: ["The percentage of requests that were I/O-bound", "Time the CPU spent performing I/O", "Idle time during which at least one I/O was outstanding"], answer: 2,
    explain: "It is idle time, so it goes to zero on a busy machine even when storage is the bottleneck — low iowait proves nothing." },
  { q: "What does high %steal on a cloud VM indicate?", options: ["The hypervisor scheduled your vCPU time elsewhere — a noisy neighbour, which no local tuning can recover", "Excessive context switching", "Memory ballooning"], answer: 0,
    explain: "The fix is a different instance type, a dedicated host, or moving — never application tuning." },
  { q: "What does /proc/pressure/io's `full` line mean?", options: ["Total I/O throughput", "The I/O queue is full", "The percentage of time during which EVERY task was stalled waiting for I/O — the machine accomplished nothing"], answer: 2,
    explain: "`some` means at least one task stalled. PSI measures saturation directly, which utilisation cannot." },
  { q: "Why should you alert on PSI rather than CPU utilisation?", options: ["PSI is cheaper to collect", "CPU utilisation is inaccurate in containers", "Utilisation is uncorrelated with user pain in both directions — a batch job at 100% is fine, a service at 40% can be badly degraded"], answer: 2,
    explain: "PSI captures queueing, which is what latency is made of. Keep utilisation for capacity planning, where it belongs." },
  { q: "In `perf stat` output, what does an IPC of 0.26 tell you?", options: ["The CPU is nearly idle", "The CPU is stalled most of the time waiting for memory — a data-layout problem, not an algorithm problem", "The profile sampling rate is too low"], answer: 1,
    explain: "Modern CPUs retire 2–4 instructions per cycle when working well. A high cache-miss rate confirms it, and no micro-optimisation of the hot function will help." },
  { q: "Why is `perf` a poor profiler for a Python or Java application?", options: ["It cannot attach to running processes", "It requires root", "It shows interpreter and JIT frames rather than application-level functions"], answer: 2,
    explain: "Use py-spy, async-profiler, or pprof — runtime-aware profilers that map samples back to application code." },
  { q: "A request takes 2 seconds but uses 20 ms of CPU. What does a CPU profile tell you?", options: ["Everything you need", "That the CPU is throttled", "Almost nothing — the other 1.98 s is off-CPU blocking, which needs offcputime or perf sched latency"], answer: 2,
    explain: "Latency = on-CPU + off-CPU, and most production latency lives in the off-CPU half. Comparing CPU time to wall time is the quickest test." },
  { q: "What is a container, at the kernel level?", options: ["A normal process with namespaces (what it sees), cgroups (what it uses), and capabilities/seccomp (what it may do)", "A lightweight virtual machine", "A chroot with a private network stack"], answer: 0,
    explain: "The kernel has no container object at all. The shared kernel is the fundamental security difference from a VM." },
  { q: "Why is root inside a Docker container by default the same as root on the host?", options: ["Docker runs everything as root deliberately", "Capabilities are not dropped", "No user namespace is enabled by default, so UID 0 inside maps to UID 0 outside"], answer: 2,
    explain: "`unshare --user --map-root-user` demonstrates the alternative: root inside, unprivileged UID outside, so an escape yields an ordinary account." },
  { q: "A service averages 30% CPU but has p99 latency spikes in a container. What should you check?", options: ["cpu.stat's nr_throttled — CFS quota is enforced per 100 ms period, so a burst exhausts it and freezes the process", "Memory limits", "Disk latency"], answer: 0,
    explain: "Prefer cpu.weight (Kubernetes requests) for latency-sensitive services; it guarantees a share under contention without ever freezing the process." },
  { q: "Why does `RUN rm secrets.txt` fail to remove a secret from a Docker image?", options: ["The build cache retains it", "rm needs --force in a Dockerfile", "Overlayfs writes a whiteout marker in the upper layer — the earlier layer still contains the file"], answer: 2,
    explain: "Use multi-stage builds so the secret never reaches the final stage, or BuildKit secret mounts which are never written to a layer." },
  { q: "Why is chroot not a security boundary?", options: ["It does not isolate the filesystem at all", "A root process can escape it in a few lines by chrooting again with its cwd outside the new root", "It can be disabled by an environment variable"], answer: 1,
    explain: "Real runtimes use pivot_root, which detaches the old root entirely — and this is another reason not to run as root inside a container." },
]},
{ title: "Quiz 10 · Mixed Mock Exam",
  desc: "Twenty questions drawn from the whole track — Foundations through Production Skills.",
  questions: [
  { q: "You run `rm -rf \"$DIR/\"` and DIR is unset. What happens, and what prevents it?", options: ["Nothing happens; the shell errors out", "The command deletes the current directory", "It expands to `rm -rf /`; set -u and \"${DIR:?}\" prevent it"], answer: 2,
    explain: "This has destroyed production systems and shipped in released installers. `\"${DIR:?}\"` in every destructive command should be a reflex." },
  { q: "`df` shows 99% full, `du` shows 2 GB used, and `df -i` shows 12% inodes. What is the cause?", options: ["Inode exhaustion", "A deleted file still held open by a process — find it with `lsof +L1`", "Filesystem corruption requiring fsck"], answer: 1,
    explain: "Truncate through /proc/PID/fd/N to reclaim the space without restarting, and use `: > file` instead of rm on live logs in future." },
  { q: "A service is unreachable from another host. `nc` reports 'Connection refused' instantly. What is ruled out?", options: ["DNS, routing and firewall — the host replied with RST, so packets got there and came back", "The service being down", "Everything except DNS"], answer: 0,
    explain: "Next step is `ss -tlnp` on the server: either nothing is listening, or it is bound to 127.0.0.1 instead of 0.0.0.0." },
  { q: "A container is OOM-killed while the host has 8 GB free. Where is the definitive evidence?", options: ["The application's own logs", "free -h output on the host", "memory.events in the container's cgroup, plus dmesg saying 'Memory cgroup out of memory'"], answer: 2,
    explain: "SIGKILL cannot be caught, so application logs show nothing. Exit status 137 (128+9) is the other tell." },
  { q: "Load average is 42 on an 8-core box, but %usr is 3% and %iowait is 87%. What is the bottleneck?", options: ["Memory pressure", "I/O — the load consists of D-state blocked processes, and more CPUs would change nothing", "CPU — add cores"], answer: 1,
    explain: "`vmstat`'s b column confirms it. Linux counts uninterruptible sleep in the load average, which is why it cannot be read as CPU demand." },
  { q: "Which single change most reduces a new server's risk of compromise?", options: ["SSH keys only with PermitRootLogin no", "Enabling SELinux in enforcing mode", "Installing an intrusion detection system"], answer: 0,
    explain: "Credential brute-force is the most common intrusion route by a wide margin, and disabling passwords eliminates it in five minutes." },
  { q: "Why should you `sshd -t` and reload rather than restart after editing sshd_config?", options: ["Restart clears the host keys", "Restart is slower", "-t catches syntax errors before they lock you out, and reload does not drop existing connections"], answer: 2,
    explain: "Also verify in a second terminal while the first stays open — on a cloud VM with no console, a lockout is unrecoverable." },
  { q: "A deploy script prints 'deployed successfully' after curl received an HTTP 500. What is missing?", options: ["set -e alone", "A longer timeout", "`curl -f`, which makes an HTTP error a non-zero exit instead of writing the error page as data"], answer: 2,
    explain: "Without -f, curl saves the error body and exits 0, so the script cheerfully extracts and deploys an error page." },
  { q: "Which command shows the effective SSH configuration including drop-ins and defaults?", options: ["systemctl show ssh", "sshd -T", "cat /etc/ssh/sshd_config"], answer: 1,
    explain: "The main file often does not reflect reality once drop-ins exist, and defaults are invisible in it entirely." },
  { q: "Two Postgres workers each show 400 MB RSS. How much physical memory are they actually using?", options: ["Exactly 400 MB total", "Less — RSS counts shared pages fully in each process; PSS from smaps_rollup gives the honest figure", "800 MB"], answer: 1,
    explain: "Summing RSS across forked workers routinely implies gigabytes that do not exist, because shared libraries and COW pages are double-counted." },
  { q: "You need a periodic job that never overlaps with itself and logs where you can find it. What do you use?", options: ["A cron entry with a lock file check", "A systemd timer plus service unit", "A background loop started at boot"], answer: 1,
    explain: "Timers give journal output, guaranteed non-overlap, Persistent=true for missed runs, resource limits, and a service half you can test with `systemctl start`." },
  { q: "A service takes 45 seconds to become ready but systemd reports it started immediately. Why?", options: ["Type=simple marks it started at fork time; Type=notify would report genuine readiness", "daemon-reload was not run", "The unit has no [Install] section"], answer: 0,
    explain: "Everything ordered After= it then races against a service that cannot yet serve — an intermittent failure that usually only appears at boot." },
  { q: "What is the correct sequence for a safe file update?", options: ["Lock the file, write in place, unlock", "Write in place then call sync", "Write temp → fsync temp → rename over target → fsync the directory"], answer: 2,
    explain: "rename() is atomic within a filesystem, so readers see the old or new file but never a partial one. The final directory fsync is the commonly-omitted step." },
  { q: "Your script iterates files with `for f in $(ls)` and one is named '-rf.txt'. What is the danger?", options: ["The loop skips it", "Unquoted, it is passed to the next command as a FLAG rather than a filename", "ls fails with an error"], answer: 1,
    explain: "Use `for f in ./*` — the ./ prefix prevents any filename being interpreted as an option, and quoting prevents splitting and globbing." },
  { q: "Which metric best predicts user-visible pain on a busy host?", options: ["CPU utilisation percentage", "Free memory", "Saturation — PSI stall time and latency percentiles"], answer: 2,
    explain: "A resource at 100% utilisation with no queueing is efficient; a resource at 40% with a deep queue is destroying your p99." },
  { q: "You suspect a host is compromised. What must you NOT do first?", options: ["Copy logs to another machine", "Reboot or power it off — that destroys memory-resident evidence", "Firewall it off"], answer: 1,
    explain: "Isolate, capture volatile state (ps, ss, lsof, /proc/*/exe), copy evidence off-host, then rebuild from a known-good image rather than cleaning." },
  { q: "Why is membership of the `docker` group equivalent to root?", options: ["Docker daemons run as root, but membership is still restricted", "A member can start a privileged container mounting the host filesystem — unrestricted root, with no sudo audit trail", "It grants CAP_SYS_ADMIN directly"], answer: 1,
    explain: "It should be audited exactly like sudo. Rootless Podman or a scoped API proxy are the alternatives." },
  { q: "A pipeline's first command fails but the script continues. Which option would have caught it?", options: ["set -e alone", "set -u", "set -o pipefail"], answer: 2,
    explain: "By default a pipeline's status is that of the LAST command only, so `false | true` reports success even under set -e." },
  { q: "Which is the strongest argument for a service logging to stdout rather than its own file?", options: ["It uses less disk", "It is faster", "Rotation, reopen signals, log-directory permissions and descriptor leaks all stop being problems, and the journal adds unit/PID/boot metadata"], answer: 2,
    explain: "It eliminates the whole class of bugs — including the classic empty-log-after-midnight rotation failure — rather than mitigating them." },
  { q: "What single check would have caught 'disk full' before the outage, that most monitoring misses?", options: ["Checking df -h more often", "Monitoring df -i (inodes) as well as bytes", "Alerting on write latency"], answer: 1,
    explain: "A filesystem can be at 30% bytes and 100% inodes, and only the second command reveals it — the alert almost nobody configures." },
]},
);
