// docker quiz bank — sets 6–10 (20 questions each, with explanations)
window.QUIZ_SETS = window.QUIZ_SETS || [];
window.QUIZ_SETS.push(
{ title: "Quiz 6 · Environment, Config & Secrets",
  desc: "The four env-var leak paths, the _FILE convention, precedence, rotation",
  questions: [
  { q: "Configuration should come from…", options: ["The Dockerfile only", "The environment at runtime (12-factor)", "Baked into the image"], answer: 1,
    explain: "One image runs unchanged across environments." },
  { q: "Putting a secret in ENV in a Dockerfile…", options: ["Is secure", "Encrypts it", "Stores it in a readable image layer"], answer: 2,
    explain: "Anyone with the image can read it." },
  { q: "A build ARG secret leaks via…", options: ["The network", "docker history", "Nothing"], answer: 1,
    explain: "ARGs appear in the image history." },
  { q: "COPY .env into an image is bad because…", options: ["It fails", "It's slow", "The secret becomes a permanent layer"], answer: 2,
    explain: "Even deleting it later leaves it in the earlier layer." },
  { q: "A secret that was committed to Git and then deleted in a later commit…", options: ["Must be treated as compromised and ROTATED — the value is in every clone, fork and cached view of the history", "Is fine if the repo is private", "Is safely removed"], answer: 0,
    explain: "The same logic applies to image layers: rebuilding without the secret is not remediation, because the old image has already been pulled." },
  { q: "The golden rule for secrets is…", options: ["Bake at build", "Inject at RUNTIME, never bake into the image", "Use ARG"], answer: 1,
    explain: "Images are distributed and may be public." },
  { q: "The `_FILE` convention (e.g. `POSTGRES_PASSWORD_FILE=/run/secrets/pgpass`) helps because…", options: ["It puts a PATH rather than the credential in `docker inspect`, keeps it out of /proc and out of child environments, and lets filesystem permissions apply", "It is faster to read", "It encrypts the value"], answer: 0,
    explain: "Official images (postgres, mysql) already support it, and implementing it yourself is a few lines with a dev fallback." },
  { q: "BuildKit's --mount=type=secret…", options: ["Exposes it transiently to one RUN, not persisted in a layer", "Bakes the secret in", "Is insecure"], answer: 0,
    explain: "For build-time secrets like a private token." },
  { q: "Environment variables leak in four places people rarely check. Which is NOT one of them?", options: ["The image's layer checksums", "`docker inspect` output", "`/proc/1/environ`, readable by any process in the container"], answer: 0,
    explain: "The four are: docker inspect, /proc/PID/environ, inherited child environments, and framework crash/debug pages that print the environment." },
  { q: "Production secrets typically live in…", options: ["The Dockerfile", "A secrets manager (Vault/AWS/GCP) or k8s Secrets", "Git"], answer: 1,
    explain: "Central store with rotation and audit." },
  { q: "Kubernetes Secrets by default are…", options: ["Base64-encoded (enable encryption-at-rest)", "Plaintext files in git", "Encrypted"], answer: 0,
    explain: "Base64 is encoding, not encryption." },
  { q: "An entrypoint that fetches a secret from a manager and `export`s it…", options: ["Is the recommended pattern", "Puts it straight back into /proc/1/environ — write it to a tmpfs file and pass the path instead", "Cannot work"], answer: 1,
    explain: "Fetching short-lived credentials at start-up is right; the mistake is putting them back in the environment you were avoiding." },
  { q: "The same image across dev/staging/prod differs only by…", options: ["The Dockerfile", "Configuration injected from the environment", "The base image"], answer: 1,
    explain: "One reproducible artifact, per-env config." },
  { q: "Environment variable precedence, from lowest to highest, is…", options: ["docker run -e, then Compose, then Dockerfile ENV", "Alphabetical", "Dockerfile ENV → Compose env_file → Compose environment → docker run -e"], answer: 2,
    explain: "Also note `-e VAR` with the variable unset in your shell sets it to EMPTY, overriding the image default rather than falling back." },
  { q: "Best practice for credential lifetime is…", options: ["Long-lived static", "Never expire", "Short-lived, rotated, scoped"], answer: 2,
    explain: "Limits blast radius if leaked." },
  { q: "Docker/Compose secrets are mounted at…", options: ["/run/secrets", "/etc/passwd", "/app/.env in a layer"], answer: 0,
    explain: "The app reads the secret from a file." },
  { q: "Committing secrets to git is…", options: ["Encrypted", "Fine if private repo", "A leak risk — use .env (gitignored)/managers"], answer: 2,
    explain: "Repos get cloned, forked, leaked." },
  { q: "Baking per-environment config into images causes…", options: ["Nothing", "Separate images per env (drift, unreproducible)", "Reproducibility"], answer: 1,
    explain: "One image + external config instead." },
  { q: "docker inspect can reveal…", options: ["A container's env vars (why env secrets leak)", "The source code", "Nothing"], answer: 0,
    explain: "Sensitive env values are visible." },
  { q: "The safest handling for a DB password is…", options: ["Mounted file from a secrets manager at runtime", "ARG", "ENV in Dockerfile"], answer: 0,
    explain: "Out of the image, injected at runtime." },
]},
{ title: "Quiz 7 · Debugging Containers",
  desc: "Exit codes 125/126/127, exec format error, exec vs attach, refused vs timeout",
  questions: [
  { q: "A container exits with code 125. This tells you…", options: ["It ran out of memory", "The DAEMON failed and no container was ever created — re-read your flags, not the image", "Your application crashed"], answer: 1,
    explain: "125 = docker's own failure (port in use, bad mount, invalid flag). 126 = found but not executable; 127 = not found. Both of those are image problems." },
  { q: "Exit code 126 versus 127 means…", options: ["Both mean 'not found'", "126 = the command exists but is not executable (missing +x, CRLF shebang); 127 = the command was not found at all", "126 = OOM, 127 = timeout"], answer: 1,
    explain: "The distinction tells you whether to check permissions and line endings, or the path and PATH." },
  { q: "A container that 'exits immediately' usually means…", options: ["Docker is broken", "The image is missing", "Its PID 1 process crashed or finished"], answer: 2,
    explain: "A container lives only as long as its main process." },
  { q: "`exec /entrypoint.sh: no such file or directory` for a file you can plainly see in the image usually means…", options: ["The missing thing is the INTERPRETER — a CRLF shebang makes the kernel look for a program literally named `/bin/sh\\r`", "The image is corrupt", "The file has no read permission"], answer: 0,
    explain: "Check with `head -1 file | xxd`. The same message appears when a glibc binary's dynamic loader is absent on musl/Alpine — check with `ldd`." },
  { q: "To explore a crashing image interactively…", options: ["docker run -it --entrypoint sh myimage", "Delete it", "Rebuild"], answer: 0,
    explain: "Override the failing entrypoint to get a shell." },
  { q: "`docker attach` on a running production container is dangerous because…", options: ["It is slow", "It restarts the container", "It connects to PID 1's own streams, so Ctrl-C sends SIGINT to the application and kills the container (exit 130)"], answer: 2,
    explain: "Detach with Ctrl-P Ctrl-Q, and prefer `docker exec`, which starts a NEW process and cannot take the container down." },
  { q: "Exit code 143 means…", options: ["OOM", "Segfault", "SIGTERM (graceful stop)"], answer: 2,
    explain: "128 + 15." },
  { q: "To debug a distroless image that has no shell, the general answer is…", options: ["Ship the :debug variant to production", "An ephemeral sidecar sharing its namespaces: `--pid=container:X --network=container:X`, reading its filesystem via /proc/1/root", "Rebuild the image with busybox"], answer: 1,
    explain: "That gives you its processes, sockets, environment and files without changing the target — and shipping a :debug image would discard the whole benefit of distroless." },
  { q: "Two containers can't talk — first check…", options: ["Disk", "They're on the same user-defined network", "CPU"], answer: 1,
    explain: "Default bridge lacks name resolution." },
  { q: "An app unreachable even with -p may be bound to…", options: ["The registry", "0.0.0.0", "127.0.0.1 inside the container"], answer: 2,
    explain: "Bind to 0.0.0.0 to be reachable." },
  { q: "'Connection refused' versus 'connection timed out' tells you…", options: ["Both mean DNS failed", "Refused means the packet ARRIVED and nothing was listening (check the bind address and port); timed out means it never arrived (check networks and firewall)", "Nothing useful"], answer: 1,
    explain: "Reading that distinction correctly eliminates most of the guesswork — they point at opposite ends of the path." },
  { q: "docker events streams…", options: ["Logs only", "Real-time daemon events (starts, stops, OOM kills)", "Nothing"], answer: 1,
    explain: "Catch kills/deaths as they happen." },
  { q: "Distroless images are hard to debug because…", options: ["They're huge", "They have no shell (exec sh fails)", "They can't run"], answer: 1,
    explain: "Use docker/kubectl debug or a debug variant." },
  { q: "kubectl debug (Kubernetes)…", options: ["Attaches a debug container sharing the pod's namespaces", "Deletes the pod", "Scales up"], answer: 0,
    explain: "Debug shell-less pods." },
  { q: "A build not using cache is usually because…", options: ["The registry is down", "An early layer changed (reorder / check .dockerignore)", "Docker is slow"], answer: 1,
    explain: "Cache busts cascade to later layers." },
  { q: "A container that crash-loops too fast to exec into is best debugged with…", options: ["`docker attach`", "Rebuilding the image repeatedly", "`docker compose run --rm --entrypoint sh <svc>` — the real image, env, mounts and networks, with a shell instead of the crashing process"], answer: 2,
    explain: "You then run the failing command by hand and read its actual error. `docker ps` also hides the loop: it shows `Up 2 seconds` while RestartCount says 23." },
  { q: "docker logs -f --tail 100…", options: ["Rebuilds", "Follows the last 100 lines live", "Deletes logs"], answer: 1,
    explain: "Tail + follow." },
  { q: "Exit code 0 means…", options: ["OOM", "Clean/successful exit", "Crash"], answer: 1,
    explain: "The process finished normally." },
  { q: "Permission denied on a volume often means…", options: ["No network", "Docker is down", "UID mismatch (non-root USER vs host file ownership)"], answer: 2,
    explain: "Align UIDs / fix ownership." },
  { q: "A mysterious sudden death with exit 137 is…", options: ["A memory problem until proven otherwise", "A network issue", "Always a bug in Docker"], answer: 0,
    explain: "OOM-killed; raise limit or fix the leak." },
]},
{ title: "Quiz 8 · Security & Non-root Containers",
  desc: "UID 0 is host root, capabilities, --privileged, read-only roots, base attack surface",
  questions: [
  { q: "Without user namespaces, a file created by a container running as root on a bind mount is owned by…", options: ["Host UID 0 — container root IS host root, which makes any bind mount a host write primitive", "The container's own namespace only", "An unprivileged mapped UID"], answer: 0,
    explain: "Verified: the file is unwritable by your own user afterwards. Appending a UID 0 line to a mounted /etc/passwd takes one command." },
  { q: "Root in the container maps to…", options: ["Root on the host kernel", "nobody", "A random user"], answer: 0,
    explain: "Shared kernel — escapes are dangerous." },
  { q: "`USER 1000` is preferred over `USER node` because…", options: ["Names break docker exec", "Kubernetes' `runAsNonRoot` cannot resolve names — it reads the image's numeric UID field", "It is shorter"], answer: 1,
    explain: "`USER 1000:1000` is better still, pinning the group as well. Place it after your installs, since it applies to every later RUN." },
  { q: "--privileged is dangerous because…", options: ["It's deprecated", "It grants near-full host access", "It's slow"], answer: 1,
    explain: "Almost never needed in prod." },
  { q: "Docker grants 14 of ~40 Linux capabilities by default. For most web services, `--cap-drop=ALL` and adding nothing back…", options: ["Requires --privileged", "Works — nginx, Postgres, Redis and Node were all measured running fine with zero capabilities", "Breaks the container"], answer: 1,
    explain: "The defaults exist so `ping` and legacy init scripts work. Drop all, run the test suite, and add back only what the failure names." },
  { q: "--cap-drop ALL then add-only-needed…", options: ["Is pointless", "Breaks everything", "Limits what even root-in-container can do"], answer: 2,
    explain: "Least privilege on Linux capabilities." },
  { q: "`NET_RAW`, granted by default, is worth dropping because it enables…", options: ["Raw sockets — and therefore ARP spoofing of other containers on the same bridge", "Reading host files", "Loading kernel modules"], answer: 0,
    explain: "SYS_ADMIN, SYS_MODULE, DAC_READ_SEARCH and SYS_PTRACE are the other high-risk ones; SYS_ADMIN is effectively an escape primitive." },
  { q: "Image CVE scanning tools include…", options: ["git", "npm", "Trivy, Grype, Snyk, docker scout"], answer: 2,
    explain: "Gate CI on HIGH/CRITICAL findings." },
  { q: "Measured, `--privileged` changes capabilities from 14 to 40, seccomp from filter-active to none, AppArmor from enforce to unconfined, and…", options: ["Nothing else", "Adds a second kernel", "Exposes ALL host devices in /dev — two commands then mount the host's disk and read /etc/shadow"], answer: 2,
    explain: "Nothing is exploited there; the door is opened. Every real need has a narrow flag: --device, --sysctl, --cap-add=SYS_PTRACE, --cap-add=NET_BIND_SERVICE." },
  { q: "no-new-privileges:true prevents…", options: ["Volumes", "Privilege escalation (e.g., via setuid)", "Networking"], answer: 1,
    explain: "A key runtime hardening flag." },
  { q: "In Kubernetes, hardening is set via…", options: ["The registry", "securityContext (runAsNonRoot, readOnlyRootFilesystem, drop caps)", "Dockerfile only"], answer: 1,
    explain: "Pod/container security controls." },
  { q: "seccomp/AppArmor/SELinux…", options: ["Speed up builds", "Are registries", "Restrict syscalls / enforce mandatory access control"], answer: 2,
    explain: "Extra kernel-level confinement." },
  { q: "`--read-only` with `noexec,nosuid` tmpfs for writable paths blocks…", options: ["Only network access", "Persistence and execution: no webshell, no cron entry, and a dropped binary cannot run — but reading files and network exfiltration are unaffected", "All attacks"], answer: 1,
    explain: "It is the best protection-per-effort control available: one flag plus the list of writable paths, which a single failing run reveals." },
  { q: "For hostile multi-tenant workloads, consider…", options: ["VM-based sandboxes (gVisor, Kata, Firecracker)", "Running as root", "--privileged"], answer: 0,
    explain: "Stronger isolation than plain containers." },
  { q: "Secrets in image layers are…", options: ["Deleted automatically", "Encrypted", "Readable by anyone with the image"], answer: 2,
    explain: "Inject at runtime instead." },
  { q: "Pinning base images by digest helps…", options: ["Reproducibility + supply-chain integrity", "Speed", "Nothing"], answer: 0,
    explain: "Immutable content reference." },
  { q: "Measured HIGH/CRITICAL findings by base image: ubuntu 17, debian-slim 23, alpine 1, distroless…", options: ["Not scannable", "0 — and distroless has no shell to spawn and no package manager to install with", "Higher than alpine"], answer: 1,
    explain: "The cost is that `docker exec sh` fails, so the healthcheck must be the app binary and debugging needs a namespace-sharing sidecar." },
  { q: "Many official images ship…", options: ["No users", "A non-root user (e.g., 'node') you can USE", "Only root"], answer: 1,
    explain: "Use it via USER." },
  { q: "Scanning should run…", options: ["Never", "Only in prod after deploy", "In CI, failing the build on critical CVEs"], answer: 2,
    explain: "Block vulnerable images before deploy." },
  { q: "Running as root + writable FS + all caps + host mounts =", options: ["Faster", "Trivial host takeover after any RCE", "Secure"], answer: 1,
    explain: "The anti-pattern to avoid entirely." },
]},
{ title: "Quiz 9 · Resource Limits, Logging & Observability",
  desc: "OOM kills, page-cache accounting, CFS throttling, log rotation, buffering",
  questions: [
  { q: "A container with no `--memory` limit that leaks memory causes…", options: ["A host-wide OOM kill that picks by oom_score — often killing the database rather than the leaking service", "A graceful restart", "Only its own death"], answer: 0,
    explain: "Limits localise damage: with --memory set, the kill happens inside the container's own cgroup and can only select processes there." },
  { q: "`docker stats` shows a sleeping container at 78% of its memory limit. The usual explanation is…", options: ["A memory leak", "A miscalculation by Docker", "Page cache — cgroup accounting includes reclaimable file cache; the real figure is `memory.current − inactive_file`, measured at 1.2 MB"], answer: 2,
    explain: "Alert on OOM kill events, not on a memory percentage that page cache inflates, or the team mutes the rule and loses the real alerts with it." },
  { q: "`--memory-swap=512m` alongside `-m 512m` means…", options: ["NO swap — --memory-swap is the COMBINED RAM+swap ceiling, so setting it equal to --memory disables swapping", "512 MB of swap in addition", "Unlimited swap"], answer: 0,
    explain: "Disabling swap is usually right for services: a container that swaps is dying slowly with terrible latency. Fail fast and let the restart policy act." },
  { q: "`--cpus=1.5` differs from `--cpu-shares=512` in that…", options: ["--cpus is a hard quota that throttles even on a completely idle host; shares are a weight that does nothing until there is contention", "They are equivalent", "Shares are a hard cap"], answer: 0,
    explain: "Quota buys predictability at the cost of tail latency; weight allows bursting at the cost of predictability." },
  { q: "A service shows 62% average CPU and a p99 of 488 ms. The counter that reveals the cause is…", options: ["pids.current", "cpu.stat's nr_throttled — 37% of periods hit the quota, invisible in any average", "memory.stat"], answer: 1,
    explain: "A multithreaded process burns 100 ms of quota in 25 ms of wall time, then every thread freezes for 75 ms. Target under ~1% of periods throttled." },
  { q: "Inside a container limited to 2 CPUs and 512 MB, `nproc` and `free` report…", options: ["The container's limits", "Zero", "The HOST's values — /proc/cpuinfo and /proc/meminfo are not namespaced"], answer: 2,
    explain: "Modern JVMs and .NET read the cgroup; Go, Node, Python and nginx do not. Use automaxprocs, --max-old-space-size and explicit worker counts." },
  { q: "A Java service with `-Xmx512m` is OOM-killed in a 512 MB container because…", options: ["The flag is ignored", "512 MB is below the JVM minimum", "The heap is not the process: metaspace, thread stacks, code cache, direct buffers and GC structures all live outside -Xmx"], answer: 2,
    explain: "Budget 25–30% of the container limit for everything outside the managed heap, and prefer -XX:MaxRAMPercentage so heap tracks the limit automatically." },
  { q: "Setting Node's `--max-old-space-size` below the container memory limit gives you…", options: ["A diagnosable JavaScript OOM with a stack trace instead of a silent SIGKILL with an empty log", "Worse performance", "No difference"], answer: 0,
    explain: "This is a debuggability win independent of the memory saved: the process ends itself with an error rather than being killed without warning." },
  { q: "`--pids-limit` is worth setting because…", options: ["A fork bomb or thread leak otherwise exhausts the HOST's process table — nothing, including SSH, can fork, and recovery is a reboot", "It speeds up forking", "It limits memory"], answer: 0,
    explain: "It costs nothing, and the pids.current ramp gives minutes of warning on a thread leak before it becomes an incident." },
  { q: "`--oom-kill-disable` on a container that hits its memory limit…", options: ["FREEZES the cgroup until memory is freed — a hung container instead of a dead one", "Raises the limit automatically", "Prevents the OOM"], answer: 0,
    explain: "Letting it die and having the restart policy act is almost always better than an unresponsive container nothing will remediate." },
  { q: "A containerised application should write its logs to…", options: ["stdout and stderr only — the daemon captures both and the logging driver decides the destination", "A rotating file inside the container", "syslog from within the app"], answer: 0,
    explain: "Rotation is the driver's job. A log file inside the container is invisible to `docker logs`, lost on removal, and grows the writable layer." },
  { q: "The default `json-file` logging driver…", options: ["Discards logs on restart", "Rotates at 10 MB", "Has NO size limit — measured at 1.4 GB in one minute, and a full /var/lib/docker breaks every container and volume on the host"], answer: 2,
    explain: "Set max-size and max-file. The `local` driver is a better default: rotation on by default and under half the bytes on disk." },
  { q: "You add log rotation to `/etc/docker/daemon.json` and reload the daemon. Existing containers…", options: ["Pick it up immediately", "Keep the old unlimited config until they are RECREATED — a `docker restart` is not enough", "Are stopped"], answer: 1,
    explain: "This is why the setting belongs in the Compose file too: `compose up -d` recreates on the config-hash change and the ceiling travels with the service." },
  { q: "To reclaim disk from a live container's huge log file…", options: ["`truncate -s 0` it — deleting leaves the daemon holding an open handle, so the space is not returned", "`rm` the file", "Restart the daemon"], answer: 0,
    explain: "Verified: df was unchanged after rm and dropped immediately after truncate, with the container still running." },
  { q: "A network logging driver in its default BLOCKING mode, when the collector stalls…", options: ["Blocks the application's write() — measured taking request latency from 3 ms to 14,208 ms, with the handler stuck inside console.log()", "Buffers to disk", "Drops lines"], answer: 0,
    explain: "Use `mode=non-blocking` with a bounded buffer: lines are dropped and counted instead of the service being taken down by its own logging." },
  { q: "A Python container is SIGKILLed and `docker logs` is completely empty. The likely cause is…", options: ["The driver was set to none", "Block buffering — stdout is a pipe, not a TTY, so the final lines died in a userspace buffer. `PYTHONUNBUFFERED=1` fixes it", "Logs were rotated away"], answer: 1,
    explain: "Testing with `docker run -t` hides this perfectly, because a TTY switches the runtime to line buffering." },
  { q: "Switching to the `fluentd` or `awslogs` logging driver has the side effect that…", options: ["Logs are compressed", "`docker logs` stops working entirely — every runbook that says 'check docker logs' breaks, typically mid-incident", "Timestamps are lost"], answer: 1,
    explain: "Prefer the `local` driver plus a node agent that tails the files: the app is never coupled to the collector, and docker logs keeps working." },
  { q: "The highest-value field to include in a structured log line is…", options: ["The hostname", "The log level", "trace_id — it is what joins one request across every service into a single story"], answer: 2,
    explain: "Six lines across three services made a timeout diagnosis unambiguous. Redact secrets at the logger, not downstream." },
  { q: "The right signal to alert on for container deaths is…", options: ["Memory percentage", "Container EVENTS — health_status, die with a non-zero exit, and oom — which are free, unambiguous, and present when application logs are not", "A log search for 'error'"], answer: 1,
    explain: "A SIGKILLed process writes nothing. Anything that can kill your process cannot be observed by your process." },
  { q: "Deriving limits from measurement (memory 1.5× peak anon, CPU 1.5× p99) versus guessing produced…", options: ["Higher costs", "0 OOM kills, 0.7% throttling and 52 ms p99, versus 4 kills, 31% throttling and 488 ms", "No measurable difference"], answer: 1,
    explain: "Limits are measurements with an expiry date: re-derive quarterly, because a limit set from last year's traffic is a guess wearing a number." },
]},
{ title: "Quiz 10 · Mixed Mock Exam",
  desc: "Comprehensive review, including registry, tagging and supply chain",
  questions: [
  { q: "A container reported exit 137 and `.State.OOMKilled` is false. That means…", options: ["It was OOM-killed anyway", "A segfault", "SIGKILL from something else — most often an ignored SIGTERM whose stop timeout expired"], answer: 2,
    explain: "137 is 128+9 and is ambiguous on its own. The OOMKilled field is what distinguishes a memory limit from a signal-handling bug." },
  { q: "For DB persistence, use…", options: ["ENV", "The container FS", "A named volume (or external managed DB)"], answer: 2,
    explain: "The writable layer is ephemeral." },
  { q: "`myco/api:1.4.2` today and the same tag last week…", options: ["May be completely different images — a tag is a mutable pointer anyone with push access can move", "Cannot both exist", "Are guaranteed identical"], answer: 0,
    explain: "Pin as `myco/api:1.4.2@sha256:...`: readable and cryptographically identified. A digest that does not exist fails loudly; a moved tag succeeds quietly." },
  { q: "depends_on guarantees…", options: ["Start order only", "Readiness", "Networking"], answer: 0,
    explain: "Add healthchecks + app retries for readiness." },
  { q: "Promoting one digest through dev → staging → prod instead of rebuilding per environment…", options: ["Is slower", "Takes a measured 0.2 s and zero bytes, and guarantees production runs the TESTED bytes — a rebuild resolved a different dependency version", "Loses the signature"], answer: 1,
    explain: "It also keeps scan results and signatures valid, because they are attached to the digest that actually deploys." },
  { q: "A secret in ENV/ARG in a Dockerfile is…", options: ["Deleted", "Readable via history/inspect (leak)", "Encrypted"], answer: 1,
    explain: "Inject at runtime instead." },
  { q: "A retention policy that deletes 'untagged manifests older than 30 days' combined with digest-pinned deployments produces…", options: ["`manifest unknown` at the next node replacement, because GC deleted the digest production pins", "Faster pulls", "Nothing — they are compatible"], answer: 0,
    explain: "Two best practices that break each other. Keep a permanent prod tag on deployed digests, or feed the running set into the retention job as an exclusion list." },
  { q: "Exit code 137 =", options: ["Success", "Syntax error", "OOM/SIGKILL"], answer: 2,
    explain: "Check memory limits." },
  { q: "Image scanning finds…", options: ["Known, published CVEs in installed packages — a malicious postinstall script scanned completely clean", "Only secrets", "All malicious code"], answer: 0,
    explain: "Scanning is hygiene for the known. Typosquatting, compromised upstreams and malicious code need pinning, provenance, signing and review." },
  { q: "Never do this in production:", options: ["Use volumes", "Scan images", "--privileged / mount the docker socket"], answer: 2,
    explain: "Effectively host root." },
  { q: "An SBOM's practical value is clearest when…", options: ["Choosing a base image", "A critical CVE is announced — querying stored SBOMs answered 'which of 400 images are affected' in 6.8 s versus 41 minutes to pull and scan", "Building the image"], answer: 1,
    explain: "Provenance answers the different question: which repo, which commit, which builder produced this image." },
  { q: "A container = a process +", options: ["a guest OS", "a hypervisor", "namespaces + cgroups + union FS"], answer: 2,
    explain: "Kernel features around a process." },
  { q: "Signing images without verifying signatures at deploy time…", options: ["Is required by OCI", "Still prevents tampering", "Changes nothing — an attacker simply pushes unsigned. The control is the admission check that refuses unsigned images"], answer: 2,
    explain: "Resolve the tag to a digest, verify THAT digest, and deploy that same digest — verifying a tag then deploying the tag leaves a window for the pointer to move." },
  { q: "For the smallest image with a Go binary…", options: ["scratch/distroless (multi-stage)", "node:22", "ubuntu"], answer: 0,
    explain: "Ship just the binary." },
  { q: "Docker Hub's anonymous pull limit of 100 per 6 hours applies…", options: ["Per user account", "Per SOURCE IP — so one NAT gateway shares one budget and an entire CI fleet fails at once with `toomanyrequests`", "Per image"], answer: 1,
    explain: "Authenticate, add a pull-through cache (which also made warm pulls 7× faster), or mirror base images into your own registry by digest." },
  { q: "Multi-container apps get name-based DNS via…", options: ["The default bridge", "Host mode", "A user-defined network (or Compose)"], answer: 2,
    explain: "No hardcoded IPs." },
  { q: "`~/.docker/config.json` stores registry credentials…", options: ["Encrypted with your login password", "Base64-encoded — anything that can read the file recovers the password in one command", "Hashed"], answer: 1,
    explain: "Use a credential helper backed by the OS keychain, or better, OIDC federation to short-lived tokens so no stored credential exists." },
  { q: "EXPOSE without -p…", options: ["Only documents it", "Publishes the port", "Opens a firewall"], answer: 0,
    explain: "Publishing needs -p." },
  { q: "A single-platform image pushed to a tag that used to be a multi-arch index…", options: ["Silently removes the other architecture, so the failure appears elsewhere as `exec format error`", "Serves both architectures anyway", "Is rejected by the registry"], answer: 0,
    explain: "Build multi-platform with `buildx --platform` in CI, and never push an image built on a laptop." },
  { q: "Scan images for CVEs…", options: ["Only after an incident", "In CI, failing on critical findings", "Never"], answer: 1,
    explain: "Block vulnerable images pre-deploy." },
]},
);
