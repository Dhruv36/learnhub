// typescript quiz bank — sets 6–10 (20 questions each, with explanations)
window.QUIZ_SETS = window.QUIZ_SETS || [];
window.QUIZ_SETS.push(
{ title: "Quiz 6 · Validation, Errors & Branded Types",
  desc: "The boundary: parsing external data, typed failures, and nominal types",
  questions: [
  { q: "Why is `JSON.parse(body) as User` unsafe?", options: ["It is slow", "It only works on arrays", "Neither half checks anything — `any` in, an unverified claim out"], answer: 2,
    explain: "`JSON.parse` returns `any` and `as` is an assertion. Both are erased, so the emitted code contains no check at all." },
  { q: "What should external data be typed as when it enters the program?", options: ["`any`", "The expected shape, asserted", "`unknown`"], answer: 2,
    explain: "`unknown` refuses every operation until narrowed, which forces the check the compiler can actually see." },
  { q: "A type predicate whose body checks only `typeof v === \"object\" && v !== null`…", options: ["Compiles clean and certifies anything non-null", "Warns under strict mode", "Fails to compile"], answer: 0,
    explain: "Verified end to end: it type-checks with zero errors, emits, and then throws `TypeError: raw.id.toUpperCase is not a function`." },
  { q: "The main advantage of a schema library over a hand-written predicate is…", options: ["It works without TypeScript", "It is faster", "The type is derived, so it cannot drift from the check"], answer: 2,
    explain: "`type User = z.infer<typeof User>` means adding a field changes the type and breaks every consumer that must handle it." },
  { q: "Declaring `interface User` alongside a separate schema object…", options: ["Reintroduces exactly the drift the schema was meant to remove", "Is the recommended pattern", "Improves inference"], answer: 0,
    explain: "Two truths related only by intention. Derive the type from the schema, or assert the schema satisfies the interface." },
  { q: "`safeParse` returns a discriminated union, which matters because…", options: ["The failure branch cannot be skipped silently", "It is faster than `parse`", "It avoids allocation"], answer: 0,
    explain: "Verified as `ZodSafeParseResult<T>`, whose success branch carries the typed data and whose failure branch carries a `ZodError` with field paths." },
  { q: "Where should validation happen?", options: ["Only in tests", "At every function call", "Once, at each named boundary"], answer: 2,
    explain: "Scattered validation costs as much as thorough validation and buys none of the confidence — nothing inside can be trusted." },
  { q: "Validating environment variables at startup matters because…", options: ["Otherwise a missing variable surfaces as an obscure failure hours after deploy", "It speeds up the process", "It is required by Node"], answer: 0,
    explain: "Parsing `process.env` at import time makes the process refuse to start and name the variable." },
  { q: "Why is `catch (e)` typed `unknown`?", options: ["JavaScript allows anything to be thrown, and no signature declares what", "Because errors are always objects", "It is a bug in the compiler"], answer: 0,
    explain: "`throw \"a string\"`, `throw 42` and `throw { code: 1 }` all compile, and there is no `throws` clause anywhere." },
  { q: "`catch (e: Error)` gives which error?", options: ["TS2322", "TS1196 — the annotation must be `any` or `unknown`", "None"], answer: 1,
    explain: "An annotation there would be an unverifiable claim, which is exactly what `useUnknownInCatchVariables` exists to prevent." },
  { q: "After narrowing `e` to `Error`, `e.cause` has type…", options: ["`Error`", "`Error | undefined`", "`unknown`"], answer: 2,
    explain: "Verified. Anything can be thrown, so anything can be a cause — walking a chain requires narrowing at each step." },
  { q: "In TypeScript 7, `Object.setPrototypeOf(this, new.target.prototype)` in a custom Error is…", options: ["Required for instanceof to work", "Unnecessary — the ES5 target was removed", "Required only for abstract classes"], answer: 1,
    explain: "`--target es5` now gives TS5108. Verified by running ES2015 output: `instanceof AppError` and `instanceof Error` are both true." },
  { q: "Setting `this.name` in a custom error matters because…", options: ["It is required by the constructor", "instanceof depends on it", "Otherwise stacks and logs say `Error` rather than the class name"], answer: 2,
    explain: "Verified: the stack's first line reads `AppError: save failed` only once `name` is set." },
  { q: "The main advantage of `Result<T, E>` over throwing is…", options: ["The failure is visible in the signature and cannot be skipped", "It works with async only", "Better performance"], answer: 0,
    explain: "A discriminated union makes ignoring the failure branch a compile error. An exception is invisible in every signature it passes through." },
  { q: "The main cost of adopting `Result` everywhere is…", options: ["It does not propagate — every intermediate function unwraps and re-wraps", "It is slower", "It breaks async/await"], answer: 0,
    explain: "TypeScript has no `?` operator, and every library edge throws anyway, so global adoption becomes plumbing." },
  { q: "`fetch` rejects when the server returns 500?", options: ["Yes", "Only under strict mode", "No — it resolves; only network failure rejects"], answer: 2,
    explain: "So the error path is never taken unless `res.ok` is checked, and both throwing and Result strategies fail identically without it." },
  { q: "`type UserId = string & { readonly __brand: \"UserId\" }`. Which assignment compiles?", options: ["`const s: string = uid`", "`const u: UserId = \"raw\"`", "`const o: OrderId = uid`"], answer: 0,
    explain: "Assignability is one-way. Verified: branded to base compiles; base to branded and brand to brand are both TS2322." },
  { q: "After `uid.toUpperCase()`, the result's type is…", options: ["`never`", "`string`", "`UserId`"], answer: 1,
    explain: "Verified — string operations strip the brand. The brand guards boundaries, not transformations." },
  { q: "Adding two differently-branded numbers, `Cents + Meters`…", options: ["Produces `Cents`", "Compiles with no error, producing a plain `number`", "Is an error"], answer: 1,
    explain: "Verified. `+` operates on the number side of the intersection, which is why branded numbers do not deliver unit safety in calculations." },
  { q: "A branded type's guarantee rests on…", options: ["Compiler enforcement of the brand", "Confining the one `as` to a smart constructor", "A runtime check of the brand property"], answer: 1,
    explain: "`\"anything\" as Email` compiles anywhere. The convention is what the compiler then leverages — hence one audited module per branded type." },
]},
{ title: "Quiz 7 · tsconfig & Modules",
  desc: "Compiler options, the module/resolution pair, imports and interop",
  questions: [
  { q: "Which two options does TypeScript 7's `tsc --init` enable that `strict` does not?", options: ["`noUncheckedIndexedAccess` and `exactOptionalPropertyTypes`", "`noImplicitAny` and `strictNullChecks`", "`skipLibCheck` and `isolatedModules`"], answer: 0,
    explain: "Verified from the generated file. A new project gets them; one running an old hand-written config almost certainly does not." },
  { q: "`types: []` in a tsconfig does what?", options: ["Disables all type checking", "Removes type annotations from output", "Stops every `@types/*` package loading into the global scope"], answer: 2,
    explain: "Without it, transitive `@types` you never installed load globally — which is how a Node service believes `document` exists." },
  { q: "`target` and `lib` differ because…", options: ["`target` sets output syntax; `lib` sets which globals the compiler believes exist", "`lib` controls downlevelling", "They are aliases"], answer: 0,
    explain: "They are independent, so `lib: [\"esnext\"]` on an older runtime type-checks and then throws." },
  { q: "`--target es5` in TypeScript 7 produces…", options: ["A warning only", "TS5108 — the target has been removed", "Downlevelled ES5 output"], answer: 1,
    explain: "Verified. That also retires the old prototype workaround for subclassing built-ins." },
  { q: "`moduleResolution: node` in TypeScript 7 gives…", options: ["TS5108 — node10 has been removed", "No error", "The modern Node algorithm"], answer: 0,
    explain: "Verified; `classic` is removed too. The surviving values are `node16`/`nodenext` and `bundler`." },
  { q: "Setting `module: esnext` with `moduleResolution: node16` gives…", options: ["TS5110 — module must be Node16", "No error", "TS5023"], answer: 0,
    explain: "They are a matched pair. `bundler` resolution, by contrast, works with `esnext` or `preserve`." },
  { q: "Which is NOT an accepted `module` value in TypeScript 7?", options: ["`nodenext`", "`amd`", "`preserve`"], answer: 1,
    explain: "Verified by passing an invalid value, which makes the compiler print the accepted list — `amd`, `umd` and `system` are absent." },
  { q: "Under `module: nodenext`, a relative import without an extension gives…", options: ["No error", "TS1484", "TS2835"], answer: 2,
    explain: "\"Relative import paths need explicit file extensions… Did you mean './dep.js'?\" — the specifier describes the emitted output." },
  { q: "Importing a `.ts` file under nodenext, you write…", options: ["`\"./dep.js\"`", "`\"./dep.ts\"`", "`\"./dep\"`"], answer: 0,
    explain: "The import specifier is the runtime path. The compiler maps it back to the source when type-checking." },
  { q: "Under `verbatimModuleSyntax`, importing a type without marking it gives…", options: ["TS2835", "TS1205", "TS1484"], answer: 2,
    explain: "\"'A' is a type and must be imported using a type-only import.\" TS1205 is the `isolatedModules` re-export equivalent." },
  { q: "`isolatedModules` requires which form for re-exporting a type?", options: ["`export default A`", "`export { A }`", "`export type { A }`"], answer: 2,
    explain: "A single-file transpiler cannot tell whether `A` is a type or a value, so TS1205 asks you to say." },
  { q: "In TypeScript 7, `esModuleInterop: false`…", options: ["Requires `allowSyntheticDefaultImports`", "Is the default", "Has been removed — TS5108"], answer: 2,
    explain: "Verified, along with `allowSyntheticDefaultImports: false`. Interop is now permanent, removing a whole class of 'works in my project' reports." },
  { q: "`import * as fn from \"pkg\"` followed by `fn()`…", options: ["Requires esModuleInterop", "Fails — a namespace object is not callable", "Works for CommonJS modules"], answer: 1,
    explain: "A namespace import binds the module namespace object regardless of the underlying export. A default import is what you want." },
  { q: "`declare module \"x\"` in a file with no imports or exports…", options: ["Augments the existing module", "Is a syntax error", "Declares an ambient module from scratch, shadowing real types"], answer: 2,
    explain: "Whether it declares or augments depends on whether the containing file is a module — which is the usual cause of an augmentation appearing to wipe out a package's types." },
  { q: "Module augmentation can…", options: ["Override a member's type", "Remove members", "Only add to the existing declarations"], answer: 2,
    explain: "Declaring a member with a contradicting type is a conflict, not an override. Wrong third-party types need a wrapper module." },
  { q: "A circular import between two modules…", options: ["Is a compile error", "Type-checks fine, and may throw `ReferenceError` at run time", "Is silently broken"], answer: 1,
    explain: "Verified: zero type errors, then `ReferenceError: Cannot access 'x' before initialization` when a value is read during module init." },
  { q: "Type-only cycles between two modules are…", options: ["Harmless — nothing is emitted for them", "Rejected by the compiler", "The same problem as value cycles"], answer: 0,
    explain: "Which is why converting an import to `import type` is one of the real fixes for a cycle." },
  { q: "Reordering imports to fix a `ReferenceError` from a cycle…", options: ["Has no effect", "Is the correct fix", "Moves the symptom rather than fixing it"], answer: 2,
    explain: "Extract the shared value into a third module, or defer the read into a function called after initialisation." },
  { q: "Barrel files are discouraged mainly because…", options: ["Importing one name evaluates everything they re-export", "They cannot re-export types", "They are slow to parse"], answer: 0,
    explain: "That causes both problems at once — no tree-shaking, and accidental initialisation-order cycles." },
  { q: "`exclude` in a tsconfig…", options: ["Guarantees a file is not compiled", "Applies to node_modules only", "Only filters what `include` matched — an imported file is still compiled"], answer: 2,
    explain: "Verified reasoning: excluding `**/*.test.ts` does nothing if a source file imports a test helper. Separate projects give real separation." },
]},
{ title: "Quiz 8 · Declarations & the Build Pipeline",
  desc: "What declaration emit produces, publishing types, and splitting emit from checking",
  questions: [
  { q: "A non-exported interface used in an exported signature is…", options: ["Replaced with `any`", "An error", "Inlined into the `.d.ts`, un-exported"], answer: 2,
    explain: "Verified. Its shape becomes part of your public API even though consumers cannot import it by name." },
  { q: "`private secret = 1` appears in the emitted `.d.ts` as…", options: ["Nothing — it is removed", "`private secret;` — the slot survives, the type is elided", "`private secret: number;`"], answer: 1,
    explain: "A private member affects assignability, so it cannot be removed; its type is nobody's business." },
  { q: "An exported value with an inferred type means…", options: ["An implementation change can silently alter your public API", "The .d.ts cannot be generated", "Nothing changes in the .d.ts"], answer: 0,
    explain: "Inferred types are written out in full, which is why libraries annotate exported signatures." },
  { q: "`declare` in a `.d.ts` means…", options: ["The compiler will verify this against an implementation", "\"This exists, trust me\" — nothing is checked", "A runtime registration"], answer: 1,
    explain: "So a hand-written declaration is exactly as accurate as its author, with the same failure mode as a lying type predicate." },
  { q: "`isolatedDeclarations` exists so that…", options: ["Declarations are smaller", "Tools other than tsc can emit `.d.ts` files", "Types are checked per file"], answer: 1,
    explain: "Normally declaration emit needs the whole type-checker, which is often the slowest part of a library build." },
  { q: "Under `isolatedDeclarations`, which of these errors?", options: ["`export const h = 1;`", "`export const g = (n: number) => n * 2;`", "`export function f() { return { a: 1 }; }`"], answer: 1,
    explain: "Verified as TS9013. A function declaration's inferred return type is accepted, so the rule is narrower than 'annotate every export'." },
  { q: "TS9009 under `isolatedDeclarations` refers to…", options: ["A missing export", "A circular type", "An un-annotated accessor"], answer: 2,
    explain: "\"At least one accessor must have an explicit type annotation.\"" },
  { q: "`skipLibCheck` does what?", options: ["Skips your own type checking", "Stops declaration files being type-checked for internal consistency", "Stops declarations being used"], answer: 1,
    explain: "It never weakens checking of your own code, which is the misconception that leads people to disable it." },
  { q: "Putting `default` before `types` in an `exports` condition causes…", options: ["Consumers to silently get no declarations", "The package to fail to install", "A build error"], answer: 0,
    explain: "Conditions are matched in order. The runtime import works perfectly, which is what makes it hard to notice." },
  { q: "A `devDependency` type appearing in a public signature causes…", options: ["An unresolvable import in your shipped `.d.ts`", "Nothing", "A slower build"], answer: 0,
    explain: "And the error names your declaration file, so it reads as your package being broken." },
  { q: "The reliable test for a package's published types is…", options: ["Running tsc in the repo", "Packing the tarball and type-checking it in an empty project", "Reading the .d.ts"], answer: 1,
    explain: "Everything inside the repo has the dev dependencies installed and resolves to source — a different environment from a consumer's." },
  { q: "esbuild compiling `const n: number = \"not a number\"`…", options: ["Emits a warning", "Emits valid JS and exits 0", "Errors"], answer: 1,
    explain: "Verified. It was never asked to check anything, which is why a pipeline whose only TS step is a transpiler has no type safety." },
  { q: "Can esbuild compile an `enum`?", options: ["Yes — it emits a runtime object, much like tsc", "No", "Only const enums"], answer: 0,
    explain: "Verified. The common claim that transpilers cannot handle enums is false, and believing it leads to the wrong diagnosis." },
  { q: "For a cross-file `const enum`, tsc emits…", options: ["`export {}` for the enum module, inlining the value into the consumer", "A runtime enum object", "An error"], answer: 0,
    explain: "Verified. esbuild instead emits a real object and keeps the import — each works alone." },
  { q: "Mixing tsc's and esbuild's output for a `const enum` produces…", options: ["A compile error", "Correct behaviour", "A runtime `SyntaxError` about a missing export"], answer: 2,
    explain: "Verified: `SyntaxError: The requested module './ce.js' does not provide an export named 'E'`." },
  { q: "`erasableSyntaxOnly` reports which error code?", options: ["TS5108", "TS1205", "TS1294"], answer: 2,
    explain: "\"This syntax is not allowed when 'erasableSyntaxOnly' is enabled\" — verified for both an enum and a namespace." },
  { q: "The idiomatic replacement for an enum is…", options: ["A `Map`", "A union of string literals", "A class with static members"], answer: 1,
    explain: "It narrows better, needs no runtime object, and gives exhaustiveness checking in a switch." },
  { q: "`.tsbuildinfo` should be…", options: ["Gitignored, and cached in CI keyed on compiler version and config", "Deleted after every build", "Committed to the repository"], answer: 0,
    explain: "An unkeyed cache is the classic cause of a build that passes locally and fails in CI." },
  { q: "In a monorepo, `tsc` instead of `tsc --build`…", options: ["Fails to start", "Silently ignores project references", "Is equivalent"], answer: 1,
    explain: "Only `--build` honours references, builds dependencies in order and skips what is up to date." },
  { q: "Project references are often worth adopting mainly for…", options: ["Better error messages", "Smaller output", "Enforcing layering — an undeclared cross-package import errors"], answer: 2,
    explain: "The build-time saving is real, but conventions about layering decay while compiler errors do not." },
]},
{ title: "Quiz 9 · Classes & Async",
  desc: "Privacy, erasability, overrides, promise combinators and the void loophole",
  questions: [
  { q: "A field declared `private soft = 2` appears in `JSON.stringify(obj)`?", options: ["No", "Only under strict mode", "Yes — `private` is erased, so it is an ordinary property"], answer: 2,
    explain: "Verified: `{\"soft\":2}`. A field marked `private passwordHash` ships to the client from any endpoint returning the object." },
  { q: "`Object.keys` on an object with `#hard` and `private soft` returns…", options: ["`['soft']`", "`['#hard', 'soft']`", "`[]`"], answer: 0,
    explain: "Verified. `#` fields are not properties at all, so no key, JSON, bracket access or cast reaches them." },
  { q: "Which class member is NOT erasable?", options: ["A `static` block", "`#field`", "A parameter property"], answer: 2,
    explain: "Verified: of seven member kinds, `erasableSyntaxOnly` flagged only `constructor(public param: string)`, which generates a constructor assignment." },
  { q: "`accessor acc = 5` with `target: esnext` emits…", options: ["Nothing", "A `#`-backed getter/setter pair", "The `accessor` keyword verbatim, which Node 24 cannot parse"], answer: 2,
    explain: "Verified: `SyntaxError: Unexpected identifier 'acc'`. At `target: es2022` tsc downlevels it correctly." },
  { q: "A typo in an override name, with `noImplicitOverride` off…", options: ["Errors at compile time", "Silently creates a new method; the base implementation keeps running", "Errors at run time"], answer: 1,
    explain: "The subclass method is dead code that looks live. The flag reports TS4117 — verified, with a 'Did you mean' suggestion." },
  { q: "TS4114 means…", options: ["A member cannot have one", "A member is abstract", "A member must have an `override` modifier"], answer: 2,
    explain: "TS4117 is the reverse — a modifier on something the base class does not declare." },
  { q: "To accept an abstract class as a value, use…", options: ["`new (...args: never[]) => T`", "`typeof T`", "`abstract new (...args: never[]) => T`"], answer: 2,
    explain: "An abstract class has no callable constructor, so the plain form rejects it — correctly, if the function will call `new`." },
  { q: "A class is justified over a module of functions when…", options: ["It has more than three functions", "There is an invariant, an `instanceof` identity, or runtime-private state", "It groups related code"], answer: 1,
    explain: "A module groups code with no construction step and better tree-shaking. Those three are things a function cannot do." },
  { q: "`Promise<T>` describes…", options: ["Whether it will settle", "The resolved value only", "Both the resolved value and the rejection"], answer: 1,
    explain: "There is no slot for the rejection and no `throws` clause — so an async function that always throws still type-checks." },
  { q: "`Promise.all([n(), s()])` where those return `Promise<number>` and `Promise<string>` resolves to…", options: ["`(string | number)[]`", "`unknown[]`", "`[number, string]`"], answer: 2,
    explain: "Verified — a tuple, which is what makes destructuring type each variable correctly." },
  { q: "Passing an array variable rather than a literal to `Promise.all` gives…", options: ["`T[]` — the positional information is lost", "An error", "The same tuple"], answer: 0,
    explain: "The variable's type already lost the positions, so keep the array inline at the call site." },
  { q: "`Promise.all` rejecting on the first failure…", options: ["Retries them", "Leaves the others running", "Cancels the other promises"], answer: 1,
    explain: "No combinator cancels anything — that needs an `AbortSignal` threaded into the underlying calls." },
  { q: "`Promise.any` with every input rejecting rejects with…", options: ["`undefined`", "An `AggregateError` carrying all of them", "The first error"], answer: 1,
    explain: "Verified at run time: `AggregateError` with `errors.length === 2`." },
  { q: "`Promise.allSettled`'s `reason` field is typed…", options: ["`any`", "`Error`", "`unknown`"], answer: 0,
    explain: "So it needs narrowing like any caught value, despite the surrounding result type being precise." },
  { q: "`await 42` produces which type?", options: ["`number`", "`42`", "`Promise<42>`"], answer: 1,
    explain: "Verified — awaiting a non-promise is legal and the literal type survives." },
  { q: "`arr.forEach(async x => { … })` compiles because…", options: ["Async callbacks are special-cased", "forEach is overloaded for async", "`Promise<void>` is assignable to `void`"], answer: 2,
    explain: "The lesson-4 void-return rule. The types genuinely line up, which is why this is a lint problem rather than a type problem." },
  { q: "What did the verified `forEach(async …)` demonstration print?", options: ["`after forEach: []` — nothing was awaited", "The full array", "A rejection warning"], answer: 0,
    explain: "forEach discards every returned promise and returns immediately, detaching the work and its errors." },
  { q: "Does the compiler report a floating promise, as in `function g() { n(); }`?", options: ["No — never", "Yes, under strict", "Only for async functions"], answer: 0,
    explain: "Verified under full strict mode. Discarding a value is legal for any type, so the defence is a lint rule." },
  { q: "Which lint rules cover what the type system structurally cannot, for promises?", options: ["`await-thenable` alone", "`require-await` alone", "`no-floating-promises` and `no-misused-promises`"], answer: 2,
    explain: "Both need type information, so they require the typed ESLint configuration — which is why teams skip them." },
  { q: "A function that iterates an async source should accept…", options: ["`AsyncIterable<T>`", "`Promise<T[]>`", "`AsyncGenerator<T>`"], answer: 0,
    explain: "Wide in, specific out. Requiring `AsyncGenerator` rejects streams and every other iterable for no benefit." },
]},
{ title: "Quiz 10 · Performance, Migration & Patterns",
  desc: "Measuring builds, converting a codebase, and the patterns worth keeping",
  questions: [
  { q: "The first command to run on a slow build is…", options: ["`tsc --generateTrace`", "`tsc --noEmit --diagnostics`", "`tsc --build --force`"], answer: 1,
    explain: "Its numbers partition the problem: high Files is configuration, high Instantiations is type-level code, high parse time is program size." },
  { q: "A file containing only `export const x: number = 1;` reports roughly how many files?", options: ["0", "83 — almost all of it lib and DOM declarations", "1"], answer: 1,
    explain: "Verified, along with 58,449 lines and 35,328 types. Your code is rarely the baseline." },
  { q: "Adding `--skipLibCheck` to that one-line compile took the type count to…", options: ["35,328 — unchanged", "0", "342"], answer: 2,
    explain: "Verified: 342 types, 0 instantiations, and the total time fell from 0.319s to 0.049s." },
  { q: "Narrowing `lib` to `es2022` on top of that gave…", options: ["No further change", "58 files and 0.016s — 20× faster than baseline", "An error"], answer: 1,
    explain: "Dropping DOM removed 25 files outright. Neither change touches your own code." },
  { q: "Which number grows non-linearly with data rather than code?", options: ["Files", "Instantiations", "Lines"], answer: 1,
    explain: "A union gaining members can multiply it without anyone editing the type that consumes it." },
  { q: "Two nested distributive conditionals over a union measured 621 instantiations at n=10. At n=20?", options: ["About 700", "About 1,200", "About 2,441 — roughly quadrupled"], answer: 2,
    explain: "Verified, and 9,681 at n=40. Doubling the union quadruples the cost — the signature of nested distribution." },
  { q: "A single distributive conditional grows…", options: ["Quadratically", "Exponentially", "Linearly — roughly 2n"], answer: 2,
    explain: "Verified: 21, 81 and 321 instantiations at n=10, 40 and 160. That version is fine at any realistic union size." },
  { q: "`--generateTrace` in TypeScript 7 emits several `types_N.json` files because…", options: ["One per source file", "The native compiler type-checks in parallel across several checkers", "One per error"], answer: 1,
    explain: "Verified: four files with `checkerId` 0–3, mapped by `legend.json`." },
  { q: "A CI performance budget should measure…", options: ["Wall-clock build time", "Deterministic counts such as Files and Instantiations", "Memory only"], answer: 1,
    explain: "Time varies with the machine and produces flaky failures, and a flaky check gets deleted within a month." },
  { q: "`allowJs` without `checkJs`…", options: ["Is a syntax error", "Lets `.js` files into the program but checks nothing", "Type-checks `.js` files"], answer: 1,
    explain: "Verified: a `.js` file with contradictory JSDoc compiles silently. Turning it on can feel like progress while changing nothing." },
  { q: "JSDoc type annotations in a `.js` file are…", options: ["Fully type-checked under `checkJs` or `// @ts-check`", "Ignored", "Checked only in editors"], answer: 0,
    explain: "Verified: TS2345 for a bad argument and TS2322 for a property contradicting its `@type`." },
  { q: "The cheapest first rung of a migration is…", options: ["Renaming every file to `.ts`", "Enabling `strictNullChecks`", "`// @ts-check` on chosen `.js` files"], answer: 2,
    explain: "It enables checking for one file with `checkJs` off, so a team can start with the modules that break most often." },
  { q: "`@ts-expect-error` over a line with no error gives…", options: ["Nothing", "TS2578 — unused directive", "TS2345"], answer: 1,
    explain: "Verified. That self-cleaning property is what keeps a migration's debt count honest without manual audits." },
  { q: "Why is renaming `.js` to `.ts` sequenced last in a migration?", options: ["Renames are slow", "Tooling requires it", "A `.ts` file full of `any` is worse than a checked `.js` one"], answer: 2,
    explain: "Renaming first is the most visible activity and the least valuable — it produces a codebase that reports as TypeScript and checks nothing." },
  { q: "A migration ratchet should fail CI when the debt count…", options: ["Falls only", "Rises", "Rises or falls"], answer: 2,
    explain: "Failing on a decrease forces the committed ceiling down, so progress cannot drift back up to a stale number." },
  { q: "The exhaustiveness pattern's compile error, when a variant is unhandled, is valuable because…", options: ["It runs faster", "It prevents the switch compiling at all", "It names the exact variant that was forgotten"], answer: 2,
    explain: "Verified: TS2322 quoting the whole `{ kind: \"tri\"; … }` member." },
  { q: "Which form keeps both the key names and the value literals while still validating the shape?", options: ["A `: Record<…>` annotation", "`as const satisfies Record<…>`", "`satisfies Record<…>`"], answer: 1,
    explain: "Verified all three: the annotation loses both, `satisfies` alone keeps keys but widens values, and the combination keeps everything." },
  { q: "'Parse, don't validate' means…", options: ["Use a parser library", "A check should produce a value of a different type, not a boolean", "Validate twice"], answer: 1,
    explain: "A boolean's guarantee stops at the `if`; a parsed type carries it to every function the value reaches." },
  { q: "Modelling state as `{ loading: boolean; data?: User; error?: Error }` is worse than a discriminated union because…", options: ["It cannot be serialised", "It is slower", "It can express combinations that are not real states"], answer: 2,
    explain: "Eight representable states for four real ones, and a `data!` assertion is usually the visible symptom." },
  { q: "The honest summary of what the type system provides is…", options: ["Full verification of external data", "Runtime safety once strict mode is on", "Enforced relationships within the program, and nothing about values it has not seen"], answer: 2,
    explain: "Every failure in this track — `any`, a lying predicate, a cycle, `forEach(async)`, a const enum, `private` in JSON — is an instance of that second half." },
]},
);
